<project version="3">
  <qibuild name="libqimessaging">
    <depends buildtime="true" runtime="true" names="libqi libqitype boost openssl" />
    <depends buildtime="true" names="python gtest pthread libqiperf"             />
  </qibuild>

  <!-- doxygen -->
  <project src="dox" />
  <project src="bin/qicli/doc" />
</project>
