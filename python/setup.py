import os
from setuptools import setup, find_packages

setup(name="qi",
      version="0.1",
      description="qi bindings",
      url="https://community.aldebaran-robotics.com/doc/",
      packages = find_packages()
)
