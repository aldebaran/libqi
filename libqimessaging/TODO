== QiMessaging ToDo ==

TODO
====

XX - ALCommon: threadpool (put each request in it's own thread)
XX - ALCommon: implement introspection and various alproxy/albroker constructor
XX - ALCommon: implement pCall (blocked by future in qi::Object)
XX - ALCommon: extract http put/get from old ALNetwork, put that in behaviormanager

XX - ALMemory: make it work using a gateway (hack inside)

XX - ALProxies: generate c++ interface from XML (once)
XX - ALProxies: parse interface and generate XML from that. (use qi/qigen)
XX - ALProxies: generate proxies from XML (or use the old python-code-generator)

XX - libqi: atomics
XX - libqi: shared_ptr

XX - cleanup future in RemoteObject without answer (timeout? max count limitation?)
XX - cleanup Answer in TransportSocket that have not been read (timeout? max count limitation?)
XX - implement XMPP transport (look at webrtc and libjingle)
XX - remove exception (everywhere)
CX - future: asynchronous call for Session (dont fake it)
XX - object/struct serialization/signature
HX - TEST TEST TEST (auto and perf)
XX - test 10000 connection/deconnection
XX - test that we do not loose message
XX - error handling (be rock solid)
XX - doc (coding style)
XX - doc (qimessaging)
LX - qi::os::getIP, qi::os::getMacAddress, qi::os::getInterfaceName
XX - ServiceDirectory: use good UUID
XX - c/python bindings
CX - qt bindings (server side, complex struct, signal/slot, events)
XX - abstract transport
XX - implement binder transport
XX - private service objects (path)
XX - failover ServiceDirectory
